This project uses Bootstrap Icons via CDN (bootstrap-icons@1.11.3), so no local icon files
are required. This folder is here in case you later want to:

- Add a custom favicon (favicon.ico / favicon.svg) — referenced from <head> in index.html
- Add brand/tech logos (e.g. company logos for the Experience timeline) as SVG/PNG
